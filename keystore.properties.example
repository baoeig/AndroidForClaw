# Release 签名配置
# 复制此文件为 keystore.properties 并填入实际值

# keystore 文件路径 (相对于项目根目录)
# 例如: ../keystore.jks (上层目录)
storeFile=../keystore.jks

# keystore 密码
storePassword=your-store-password

# key 密码
keyPassword=your-key-password

# key 别名
keyAlias=your-key-alias
